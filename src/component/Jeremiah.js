import React from 'react'

export default function Jeremiah(props) {
    return (
        <div>
            <h1>{props.boy.name}</h1>
            <p>{props.boy.project}</p>
            <p>{props.boy.seat}</p>
            
        </div>
    )
}
